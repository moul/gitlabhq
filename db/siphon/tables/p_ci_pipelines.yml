---
table: p_ci_pipelines
database: ci
ignored_columns:
  - lock_version
replication_targets:
  - name: clickhouse_main
    target: siphon_p_ci_pipelines
    dedup_by:
      - id
      - partition_id
