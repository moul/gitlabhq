# @feature_category: continuous_integration
#import "~/graphql_shared/fragments/ci_icon.fragment.graphql"

subscription ciStageStatusUpdated($stageId: CiStageID!) {
  ciStageStatusUpdated(stageId: $stageId) {
    id
    name
    detailedStatus {
      ...CiIcon
      tooltip
      name
    }
  }
}
