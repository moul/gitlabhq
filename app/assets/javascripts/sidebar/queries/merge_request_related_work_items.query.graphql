# @feature_category: portfolio_management
query mergeRequestRelatedWorkItems($id: MergeRequestID!) {
  mergeRequest(id: $id) {
    id
    linkedWorkItems {
      linkType
      workItem {
        id
        iid
        title
        namespace {
          id
          fullPath
        }
      }
    }
  }
}
